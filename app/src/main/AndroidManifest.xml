<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.oruizp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".feature.paging.view.MovieDetailActivity"></activity>
        <activity android:name=".feature.paging.view.MoviesActivity" />
        <activity android:name=".feature.intentservice.IntentActivity" />
        <activity android:name=".feature.room.view.TaskDetailActivity" />
        <activity android:name=".feature.room.view.TaskActivity" />
        <activity android:name=".feature.landing.LandingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".feature.intentservice.WaterReminderIntentService"
            android:exported="false" />
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>